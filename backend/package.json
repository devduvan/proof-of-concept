{
  "name": "thumbnail-generator-api-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint .",
    "start": "./main.sh -c start -e dev",
    "deploy": "'npm run lint' && ./main.sh -c deploy -e $npm_config_env",
    "deployStack": "'npm run lint' && ./main.sh -c deploy -e $npm_config_env -s $npm_config_stack",
    "destroy": "./main.sh -c destroy -e $npm_config_env",
    "destroyStack": "./main.sh -c destroy -e $npm_config_env -s $npm_config_stack"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.6.0",
    "eslint-plugin-jest": "^25.3.4",
    "nodemon": "^2.0.15",
    "serverless-offline-multi": "^1.2.0"
  }
}
